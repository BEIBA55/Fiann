# Система управления событиями (Event Management System)

Полнофункциональное MERN приложение для управления и поиска событий, построенное на TypeScript, GraphQL, Next.js и MongoDB.

## 🎯 Обзор проекта

EventHub — это платформа для управления событиями, которая позволяет пользователям открывать, создавать и управлять событиями.

### Цели проекта
- Создать полнофункциональную систему управления событиями с поддержкой реального времени
- Обеспечить удобный интерфейс для организаторов и участников событий
- Реализовать безопасную аутентификацию и авторизацию с ролевой моделью

### Домен
**Event Management System** — система управления событиями, включающая создание, публикацию, регистрацию и взаимодействие с событиями.

### Роли пользователей
- **USER** — обычный пользователь, может просматривать события, регистрироваться на них, оставлять комментарии
- **ORGANIZER** — организатор, может создавать и управлять своими событиями, изменять их статус
- **ADMIN** — администратор, имеет полный доступ: управление всеми событиями, пользователями, изменение ролей

## 🏗️ Архитектура

### Технологический стек

**Backend:**
- Node.js + Express.js
- GraphQL (Apollo Server)
- MongoDB + Mongoose
- TypeScript
- JWT Аутентификация
- GraphQL Subscriptions (WebSocket)

**Frontend:**
- Next.js 14 (App Router)
- React 18
- TypeScript
- TailwindCSS
- Apollo Client
- Zustand (Управление состоянием)
- React Hook Form + Zod (Валидация форм)

**DevOps:**
- Docker & Docker Compose
- Jest (Тестирование)

## 📊 Схема данных

### Модели (4 основные модели)

**User (Пользователь)** - 5+ полей
- `name`, `email`, `password`, `role`, `avatar`, `isDeleted`, `createdAt`, `updatedAt`

**Event (Событие)** - 10+ полей
- `title`, `description`, `date`, `location`, `capacity`, `organizerId`, `status`, `category`, `imageUrl`, `isDeleted`, `createdAt`, `updatedAt`

**Registration (Регистрация)** - 6+ полей
- `userId`, `eventId`, `status`, `registeredAt`, `notes`, `isDeleted`, `createdAt`, `updatedAt`

**Comment (Комментарий)** - 5+ полей
- `userId`, `eventId`, `content`, `rating`, `isDeleted`, `createdAt`, `updatedAt`

### Связи между моделями
- **User 1:N Event** - один пользователь (организатор) может создать много событий
- **User N:M Event** - много пользователей могут регистрироваться на много событий (через Registration)
- **Event 1:N Registration** - одно событие может иметь много регистраций
- **Event 1:N Comment** - одно событие может иметь много комментариев
- **User 1:N Comment** - один пользователь может оставить много комментариев

## 🔌 GraphQL API

### Queries (6+)
- `me` - Получить текущего пользователя
- `users` - Список всех пользователей
- `user(id)` - Получить пользователя по ID
- `events(status, category, limit, offset)` - Список событий с фильтрами
- `event(id)` - Получить событие по ID
- `myEvents` - Получить события текущего пользователя
- `registrations(eventId, userId)` - Список регистраций
- `registration(id)` - Получить регистрацию по ID
- `myRegistrations` - Получить регистрации текущего пользователя
- `comments(eventId)` - Список комментариев для события
- `comment(id)` - Получить комментарий по ID

### Mutations (6+)
- `register(input)` - Регистрация нового пользователя
- `login(input)` - Аутентификация пользователя
- `createEvent(input)` - Создать новое событие
- `updateEvent(id, input)` - Обновить событие
- `deleteEvent(id)` - Мягкое удаление события
- `createRegistration(input)` - Зарегистрироваться на событие
- `updateRegistration(id, input)` - Обновить статус регистрации
- `cancelRegistration(id)` - Отменить регистрацию
- `createComment(input)` - Добавить комментарий
- `updateComment(id, input)` - Обновить комментарий
- `deleteComment(id)` - Удалить комментарий

### Subscriptions (3+)
- `eventCreated` - Новое событие создано
- `eventUpdated` - Событие обновлено
- `registrationCreated(eventId)` - Новая регистрация на событие
- `registrationUpdated(eventId)` - Регистрация обновлена
- `commentAdded(eventId)` - Новый комментарий к событию

## 🚀 Быстрый старт

### Требования
- Docker & Docker Compose

### Запуск через Docker (1-2 шага)

1. **Клонировать репозиторий и запустить все сервисы**
```bash
git clone <repository-url>
cd Fiann
docker-compose up
```

2. **Заполнить базу данных тестовыми данными** (опционально)
```bash
# В новом терминале
docker exec -it event-management-api npm run seed
```

После запуска доступны:
- **Frontend**: http://localhost:3000
- **GraphQL API**: http://localhost:4000/graphql
- **MongoDB**: localhost:27017

### Локальная разработка

#### Настройка Backend
```bash
cd server
npm install
cp .env.example .env
# Отредактируйте .env с вашей конфигурацией
npm run dev
```

#### Настройка Frontend
```bash
cd client
npm install
cp .env.example .env.local
# Отредактируйте .env.local с вашей конфигурацией
npm run dev
```

## 🧪 Тестирование

### Запуск тестов
```bash
cd server
npm test
```

### Покрытие тестами
```bash
cd server
npm run test:coverage
```

**Набор тестов:**
- 10+ Unit тестов для резолверов
- 1+ Интеграционный тест для потока событий
- Тесты покрывают аутентификацию, события, регистрации и комментарии

## 📱 Ключевые экраны

1. **Главная страница** (`/`) - Лендинг с навигацией
2. **Вход** (`/login`) - Аутентификация пользователя
3. **Регистрация** (`/register`) - Регистрация нового пользователя
4. **Список событий** (`/events`) - Просмотр всех опубликованных событий
5. **Детали события** (`/events/[id]`) - Просмотр события, регистрация, комментарии
6. **Создание события** (`/events/create`) - Организаторы создают события
7. **Профиль** (`/profile`) - Профиль пользователя и регистрации

## 🔄 Как проверить реалтайм (пошагово)

### Тестирование подписок GraphQL

#### Шаг 1: Подготовка
1. Запустите приложение через Docker: `docker-compose up`
2. Заполните базу данных: `docker exec -it event-management-api npm run seed`
3. Войдите в систему как пользователь (см. тестовые учетные данные ниже)

#### Шаг 2: Тест подписки на комментарии
1. Откройте страницу любого события: http://localhost:3000/events/[id]
2. Откройте ту же страницу в **втором окне/вкладке браузера** (или в режиме инкогнито)
3. В **первом окне**: Добавьте комментарий с текстом и оценкой
4. В **втором окне**: Комментарий должен появиться **автоматически без перезагрузки страницы**

#### Шаг 3: Тест подписки на регистрации
1. Откройте страницу события в двух окнах (как в шаге 2)
2. В **первом окне**: Нажмите "Зарегистрироваться на событие"
3. В **втором окне**: Счетчик регистраций должен обновиться **автоматически**

#### Шаг 4: Проверка WebSocket соединения
- Откройте консоль браузера (F12) → вкладка Network → WS
- Должно быть активное соединение: `ws://localhost:4000/graphql`
- При добавлении комментария/регистрации видно обмен сообщениями

### WebSocket Endpoint
- **Разработка**: `ws://localhost:4000/graphql`

## 🔐 Аутентификация

### Тестовые учетные данные (после заполнения базы)
- **Администратор**: `admin@example.com` / `password123`
- **Организатор**: `organizer@example.com` / `password123`
- **Пользователь**: `jane@example.com` / `password123`

### JWT Токен
- Хранится в localStorage после входа
- Отправляется в заголовке Authorization: `Bearer <token>`
- Истекает через 7 дней (настраивается)

## 📝 Переменные окружения

### Server (.env)
```env
NODE_ENV=development
PORT=4000
MONGO_URI=mongodb://mongo:27017/event-management
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:3000
```

### Client (.env.local)
```env
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000/graphql
NEXT_PUBLIC_WS_URL=ws://localhost:4000/graphql
```

## 🐳 Docker

### Сборка образов
```bash
docker-compose build
```

### Запуск сервисов
```bash
docker-compose up -d
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Остановка сервисов
```bash
docker-compose down
```

### Очистка томов
```bash
docker-compose down -v
```

## 📦 Структура проекта

```
Final/
├── server/
│   ├── src/
│   │   ├── models/          # Mongoose модели
│   │   ├── graphql/          # GraphQL схема и резолверы
│   │   ├── utils/            # Аутентификация, ошибки, валидация
│   │   ├── config/           # Конфигурация базы данных
│   │   ├── scripts/          # Скрипт заполнения базы
│   │   ├── __tests__/        # Jest тесты
│   │   └── index.ts          # Точка входа
│   ├── Dockerfile
│   └── package.json
├── client/
│   ├── app/                  # Страницы Next.js App Router
│   ├── components/           # React компоненты
│   ├── lib/                  # Apollo Client, GraphQL запросы
│   ├── store/                # Zustand хранилища
│   ├── Dockerfile
│   └── package.json
└── docker-compose.yml
```

## 🛠️ Скрипты

### Server (Backend)
```bash
cd server
npm run dev          # Запустить сервер разработки с hot-reload
npm run build        # Собрать TypeScript в JavaScript
npm start            # Запустить продакшн сервер
npm test             # Запустить все тесты (Jest)
npm run test:watch   # Запустить тесты в watch режиме
npm run test:coverage # Запустить тесты с покрытием кода
npm run seed         # Заполнить базу данных тестовыми данными
npm run lint         # Проверить код с ESLint
npm run lint:fix     # Исправить ошибки ESLint автоматически
```

### Client (Frontend)
```bash
cd client
npm run dev          # Запустить Next.js dev сервер (http://localhost:3000)
npm run build        # Собрать для продакшна
npm start            # Запустить продакшн сервер
npm run lint         # Проверить код с ESLint
```

## 🎨 Функции

- ✅ TypeScript strict mode
- ✅ GraphQL с Subscriptions
- ✅ JWT Аутентификация и авторизация
- ✅ Валидация форм (Zod + React Hook Form)
- ✅ Обновления в реальном времени (WebSocket)
- ✅ Адаптивный дизайн (TailwindCSS)
- ✅ Управление состоянием (Zustand)
- ✅ Мягкое удаление
- ✅ Обработка ошибок
- ✅ Поддержка Docker
- ✅ Комплексные тесты

## 📄 Лицензия

ISC

## 👥 Команда / Роли

**Участники проекта:**

- **Мейрамбек** 
  - Backend разработка (Node.js, Express, GraphQL)
  - Настройка MongoDB и Mongoose моделей
  - Реализация GraphQL резолверов и подписок
  - JWT аутентификация и авторизация
  - Написание тестов (Jest, unit и integration)
  - Docker конфигурация для backend

- **Бейбарс**
  - Frontend разработка (Next.js, React, TypeScript)
  - UI/UX дизайн (TailwindCSS)
  - Интеграция Apollo Client и GraphQL Subscriptions
  - Управление состоянием (Zustand)
  - Валидация форм (React Hook Form + Zod)
  - Docker конфигурация для frontend

## 🔗 Демо-ссылки

### Продакшн (Production)
- **Frontend**: https://outstanding-serenity-production.up.railway.app/
- **Backend**: https://fiann-production.up.railway.app/


### Тестовый пользователь (после seed)
После выполнения `npm run seed` доступны следующие учетные записи:

- **Администратор**: 
  - Email: `admin@example.com`
  - Пароль: `password123`
  - Роль: ADMIN (полный доступ, управление пользователями)

- **Организатор**: 
  - Email: `organizer@example.com`
  - Пароль: `password123`
  - Роль: ORGANIZER (создание и управление событиями)

- **Пользователь**: 
  - Email: `jane@example.com`
  - Пароль: `password123`
  - Роль: USER (просмотр и регистрация на события)

## 📚 Дополнительные ресурсы

- [Документация GraphQL](https://graphql.org/)
- [Документация Next.js](https://nextjs.org/docs)
- [Apollo Client](https://www.apollographql.com/docs/react/)
- [MongoDB](https://www.mongodb.com/docs/)

---

**Примечание**: Не забудьте изменить JWT_SECRET и другие секретные значения в продакшне!


**Презентация**: https://www.canva.com/design/DAG7vPyYnDg/BInlidVePMkBKuRw5BuLZQ/edit?utm_content=DAG7vPyYnDg&utm_campaign=designshare&utm_medium=link2&utm_source=sharebutton